global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    service: llm-inference-server

rule_files: []

scrape_configs:
  - job_name: gateway
    static_configs:
      - targets:
          - gateway:8000
    metrics_path: /metrics/
    scrape_interval: 15s
